# BRIEFING — Sesión 3: Shell — AgentInbox + Badge + AgentThread

**Fecha:** 2026-05-06  
**Para:** Shell Engineer (Antigravity)  
**Épica:** EPIC 50 — Agent Inbox  
**Tickets:** CORE-269, CORE-274 (parte shell), CORE-270

---

## Prerequisito

**La Sesión 2 (`feat/core-276-approved-paths-websocket`) debe estar mergeada
a `main` antes de empezar.**

El kernel ya emite frames `{ "event": "agent_event", "data": { "type": "...", ... } }`
por WebSocket. Esta sesión es la contraparte en la UI.

Leer antes de implementar:
- `governance/Tickets/CORE-269.md`
- `governance/Tickets/CORE-274.md`
- `governance/Tickets/CORE-270.md`

---

## Branch

```
feat/core-269-agent-inbox-ui
```

---

## Objetivo

### 1. CORE-274 (parte Shell) + CORE-269 — AgentInbox store + Badge

**Archivo nuevo:** `shell/ui/src/types/agentEvents.ts`

```typescript
export type AgentEventPayload =
  | {
      type: 'supervisor_question';
      agent_id: string;
      project_name: string;
      question: string;
      context?: string;
      timestamp: string;
    }
  | { type: 'supervisor_resumed'; agent_id: string }
  | {
      type: 'supervisor_completed';
      agent_id: string;
      project_name: string;
      summary: string;
    }
  | { type: 'supervisor_timed_out'; agent_id: string; project_name: string };
```

**Archivo nuevo:** `shell/ui/src/store/agentInboxStore.ts`

```typescript
import { create } from 'zustand';

export interface ThreadMessage {
  role: 'supervisor' | 'user';
  content: string;
  timestamp: string;
}

export interface AgentMessage {
  agentId: string;
  projectName: string;
  question: string;
  context?: string;
  timestamp: string;
  status: 'pending' | 'answered' | 'timed_out';
  thread: ThreadMessage[];
}

interface AgentInboxState {
  messages: AgentMessage[];
  pendingCount: number;
  addMessage: (msg: Omit<AgentMessage, 'thread'>) => void;
  markAnswered: (agentId: string) => void;
  markTimedOut: (agentId: string) => void;
  addThreadMessage: (agentId: string, msg: ThreadMessage) => void;
  getByAgentId: (agentId: string) => AgentMessage | undefined;
}

export const useAgentInboxStore = create<AgentInboxState>((set, get) => ({
  messages: [],
  pendingCount: 0,
  addMessage: (msg) =>
    set((state) => {
      const exists = state.messages.some(
        (m) => m.agentId === msg.agentId && m.status === 'pending'
      );
      if (exists) return state;
      const thread: ThreadMessage[] = [{
        role: 'supervisor',
        content: msg.question,
        timestamp: msg.timestamp,
      }];
      const updated = [...state.messages, { ...msg, thread }];
      return {
        messages: updated,
        pendingCount: updated.filter((m) => m.status === 'pending').length,
      };
    }),
  markAnswered: (agentId) =>
    set((state) => {
      const updated = state.messages.map((m) =>
        m.agentId === agentId ? { ...m, status: 'answered' as const } : m
      );
      return {
        messages: updated,
        pendingCount: updated.filter((m) => m.status === 'pending').length,
      };
    }),
  markTimedOut: (agentId) =>
    set((state) => {
      const updated = state.messages.map((m) =>
        m.agentId === agentId ? { ...m, status: 'timed_out' as const } : m
      );
      return {
        messages: updated,
        pendingCount: updated.filter((m) => m.status === 'pending').length,
      };
    }),
  addThreadMessage: (agentId, msg) =>
    set((state) => ({
      messages: state.messages.map((m) =>
        m.agentId === agentId
          ? { ...m, thread: [...m.thread, msg] }
          : m
      ),
    })),
  getByAgentId: (agentId) => get().messages.find((m) => m.agentId === agentId),
}));
```

**En el handler WebSocket existente** (donde se procesan mensajes entrantes):

Buscar el archivo que maneja el WebSocket en `shell/ui/src/` (probablemente en
`store/` o en el componente principal). Agregar parseo de `agent_event` antes
de procesar tokens de chat:

```typescript
// Al inicio del handler de mensajes WS:
try {
  const parsed = JSON.parse(rawMessage);
  if (parsed.event === 'agent_event') {
    handleAgentEvent(parsed.data as AgentEventPayload);
    return; // no procesar como token de chat
  }
} catch {
  // no es JSON o no es agent_event — continuar con procesamiento normal
}

function handleAgentEvent(data: AgentEventPayload) {
  const store = useAgentInboxStore.getState();
  switch (data.type) {
    case 'supervisor_question':
      store.addMessage({
        agentId: data.agent_id,
        projectName: data.project_name,
        question: data.question,
        context: data.context,
        timestamp: data.timestamp,
        status: 'pending',
      });
      break;
    case 'supervisor_completed':
      store.markAnswered(data.agent_id);
      break;
    case 'supervisor_timed_out':
      store.markTimedOut(data.agent_id);
      break;
    case 'supervisor_resumed':
      // no action needed aquí
      break;
  }
}
```

**Archivo nuevo:** `shell/ui/src/components/AgentBadge.tsx`

Badge que muestra el número de mensajes pendientes. Se integra en la barra
de navegación principal.

```typescript
import { useAgentInboxStore } from '../store/agentInboxStore';
import { useNavigate } from 'react-router-dom'; // o el router que use el proyecto

export function AgentBadge() {
  const pendingCount = useAgentInboxStore((s) => s.pendingCount);
  const navigate = useNavigate();

  if (pendingCount === 0) return null;

  return (
    <button
      onClick={() => navigate('/agents')}
      className="relative p-2 rounded-full hover:bg-muted transition-colors"
      title={`${pendingCount} mensaje${pendingCount > 1 ? 's' : ''} pendiente${pendingCount > 1 ? 's' : ''} de supervisores`}
    >
      {/* Usar el ícono que mejor encaje con el sistema de iconos existente */}
      <span className="text-muted-foreground text-sm">⚡</span>
      <span className="absolute -top-1 -right-1 flex h-4 w-4 items-center justify-center rounded-full bg-primary text-[10px] text-primary-foreground font-medium">
        {pendingCount > 9 ? '9+' : pendingCount}
      </span>
    </button>
  );
}
```

Integrar `AgentBadge` en la barra de navegación principal — junto a los otros
controles existentes. Ver el layout principal para encontrar el lugar correcto.

---

### 2. CORE-270 — Rutas /agents y /agents/:agentId

**Rutas nuevas** (agregar al router existente):
- `/agents` — lista de supervisores activos
- `/agents/:agentId` — hilo de conversación con un supervisor

**Archivo nuevo:** `shell/ui/src/components/AgentInboxList.tsx`

Lista de todos los supervisores en la sesión. Una fila por agente:

```
┌─────────────────────────────────────────┐
│  ⚡ Aegis-Core                           │
│  "¿Puedo ejecutar comandos en el        │
│   entorno para clonar el repositorio?"  │
│  hace 2 minutos                [Abrir →]│
├─────────────────────────────────────────┤
│  ✅ Backend API                          │
│  Tarea completada                       │
│  hace 15 minutos                        │
└─────────────────────────────────────────┘
```

Estados visuales:
- `pending` → ⚡ (o color primario / warning)
- `answered` → ✅
- `timed_out` → ⏱ con texto "Sin respuesta — el supervisor continuó"

**Archivo nuevo:** `shell/ui/src/components/AgentThread.tsx`

Vista de conversación con un supervisor. Recibe `agentId` como prop o param de ruta.

Layout:
- Header: nombre del proyecto, estado (badge de color), botón volver
- Lista de mensajes: burbujas alineadas según `role` (supervisor izquierda, user derecha)
  — mismo estilo visual que el chat principal
- Footer con input de texto + botón enviar (solo activo si `status === 'pending'`)
- Si `status === 'answered'`: input deshabilitado, mensaje "Ya respondiste"
- Si `status === 'timed_out'`: input deshabilitado, mensaje "El supervisor continuó sin respuesta"

Al enviar desde el footer, llamar `POST /api/agents/:agentId/reply` (CORE-271).
Por ahora puede mockearse con un `console.log` si CORE-271 no está mergeado aún.
Cuando llegue la respuesta OK del endpoint:
- `addThreadMessage(agentId, { role: 'user', content: answer, timestamp: now })`
- `markAnswered(agentId)`

---

## Verificación

```bash
npm run build   # en shell/ui/
npm run lint
```

Sin errores de TypeScript ni de lint.

---

## Commit y PR

**Commit message:**
```
feat(shell): CORE-269/274/270 AgentInbox store, badge, thread UI
```

**PR title:**
```
feat(shell): CORE-269/274/270 — AgentInbox store + badge + hilo dedicado
```

**PR description:**
```
## Cambios

### CORE-274 (parte Shell)
- `types/agentEvents.ts`: tipos TypeScript para AgentEventPayload
- Handler WS: parseo de frames `agent_event` con dispatch al store

### CORE-269 — AgentInbox store + badge
- `store/agentInboxStore.ts`: store Zustand con messages, pendingCount, thread por agente
- `components/AgentBadge.tsx`: badge numérico en nav, navega a /agents
- Badge integrado en la barra de navegación principal

### CORE-270 — Rutas /agents y /agents/:agentId
- Ruta `/agents`: lista de supervisores activos (AgentInboxList)
- Ruta `/agents/:agentId`: hilo de conversación con un supervisor (AgentThread)
- Input de respuesta activo solo si status === 'pending'
- Estilo visual consistente con el chat principal

## Notas
- El envío de respuesta en AgentThread llama CORE-271 endpoint
  (si no está mergeado aún, mockeado con console.log)

## Verificación
`npm run build` ✅  `npm run lint` ✅
```

**Target branch:** `main`

---

*Briefing creado por Arquitecto IA — 2026-05-06*
